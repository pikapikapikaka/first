<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".select_city">
    <RelativeLayout
        android:id="@+id/home_top"
        android:layout_width="fill_parent"
        android:layout_height="45dp"
        android:background="#4169E1">
        <TextView
            android:id="@+id/home_title_name"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="当前城市:北京"
            android:gravity="center_horizontal"
            android:textSize="30sp"
            android:textColor="#ffffff"/>
        <ImageView
            android:id="@+id/home_back"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:src="@drawable/base_action_bar_back_normal"
            android:layout_alignLeft="@+id/home_title_name"/>
        <ImageView
            android:layout_width="1dp"
            android:layout_height="45dp"
            android:layout_toRightOf="@+id/home_back"
            android:background="#000000"/>
        <ImageView
            android:id="@+id/home_search"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:src="@drawable/search"
            android:background="#000000"
            android:layout_alignRight="@+id/home_title_name"/>
    </RelativeLayout>
    <SearchView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/home_top"
        android:layout_margin="10dp"
        android:id="@+id/search"></SearchView>
    <ListView
        android:id="@+id/home_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/search"
        android:background="#ffffff"></ListView>

</RelativeLayout>